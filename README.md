# Face-Mask-Detector
This program determines if you are wearing a face mask or not
To run this program, start a localhost on your computer and host the folder. The HTML file should run
ADD YOUR IMAGE DATASET - in the images folder, add pictures of yourself with and without a mask for the program to detect and compare the images to the live camera
